#include "0-main.c"

/**
 *_isupper - check if c is upper or not
 * @c: the char to check it
 * Return: 1 if c isupper, 0 Otherwise
 */
int _isupper(int c)
{
	int c;

	if (c = 'A' && c = 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
